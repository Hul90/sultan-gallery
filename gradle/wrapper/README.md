# Gradle Wrapper

This project includes `gradlew` and `gradlew.bat` launcher scripts. They read the Gradle version from `gradle-wrapper.properties`, download the declared distribution on first use, cache it under the user's Gradle home, and execute it.
